// Waketale v2 — placeholder widget test.
// Full integration tests are in QA test plan.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
